#include "main.h"
/**
 *puts 2 - affiche un caractere sur deux
 *@str: string
 */
void puts2(char *str)
{
	int i = 0;

	while (str[i] != '\0')
	{
		if (i % 2 == 0)
			_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}
